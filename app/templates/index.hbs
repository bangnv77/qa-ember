<h1> Welcome to Q&amp;A! </h1>

<ul class="list-group">
  {{#each model as |question|}}
    <li class="list-group-item"> {{question.content}} by {{question.author}} </li>
  {{/each}}
</ul>

{{new-question sentFromNewQuestionComponent="saveAtIndexRoute"}}
